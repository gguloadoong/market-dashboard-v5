{
  "name": "mdv5-cron",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secrets:push": "wrangler secret bulk .secrets.json"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  }
}
